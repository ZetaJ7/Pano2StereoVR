# G4 Quick Summary (2026-03-03, Windows)

## Scope

This summary records the post-G3 end-to-end quickcheck with **SHM caps disabled**:

- `--shm-fps-cap 0`
- `--shm-fps-cap-fast 0`

Sources:

- Unity validation log:  
  `C:/Users/admin/AppData/LocalLow/DefaultCompany/Pano2StereoVR/g3_mode_validation.jsonl`
- Python run dir:  
  `D:/WorkSpace/Pano2Stereo/output/streaming/run_015/`

---

## Runtime Configuration

From `run_015/run_config.json`:

- `output.method = "shm"`
- `output.shm_fps_cap = 0.0`
- `output.shm_fps_cap_fast = 0.0`
- `experiment.initial_mode = 3`

---

## Mode Coverage (duration)

Derived from `run_015/experiment_log.jsonl` (`session_start` → `session_end`, total `789.531s`):

- Mode 1 total: `316.300s` (`5.27 min`)
- Mode 2 total: `183.170s` (`3.05 min`)
- Mode 3 total: `290.060s` (`4.83 min`)

All three modes exceed 2 minutes in this run.

---

## Unity Control-Loop Stability (latest session)

Session window in Unity log:
- `2026-03-03T08:32:35Z` → `2026-03-03T08:44:07Z`

Results:

- `mode_applied` count: `5`
- `mode_timeout` count: `0`
- same-mode no-op (`keyboard_already_applied`) count: `5`
- mode switch latency (ms):  
  - min: `72.290`
  - mean: `109.500`
  - max: `157.040`

Interpretation:

- The "pressing current mode causes timeout" issue remains fixed.
- `requested -> sent -> applied` chain stays functional for cross-mode switches.

---

## Python Throughput by Mode (`run_015/run.log`)

`[FPS]` samples grouped by active mode:

| Mode | Stereo FPS mean | Stereo FPS min-max | Depth FPS mean | Samples |
|---|---:|---:|---:|---:|
| 1 (MONO) | 19.396 | 14.320 ~ 24.910 | 3.451 | 63 |
| 2 (STEREO_NO_GAZE) | 13.997 | 13.430 ~ 14.720 | 3.551 | 35 |
| 3 (STEREO_WITH_GAZE) | 12.356 | 11.770 ~ 16.560 | 3.985 | 58 |

Additional note:

- `depth_drop: 0 (0.00%)` throughout sampled logs.

---

## Conclusion

For this quickcheck configuration (`shm_fps_cap=0`, `shm_fps_cap_fast=0`):

- Mode switching and Unity control-loop behavior are stable (no new timeout in latest session).
- End-to-end pipeline runs continuously with all three modes covered for >2 minutes each.
- Performance ordering is consistent with expectation: `Mode1 > Mode2 > Mode3` in stereo throughput.

Status: **G4 quick summary ready**.
